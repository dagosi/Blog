<h1><%= @post.title %></h1>
<p><%= @post.content %></p>

<p>
  <b>Tags:</b>
  <%= join_tags @post %>
</p>
<hr />
<h2 id="num_comments"><%= "Comments (#{ @post.comments.count })"%></h2>
<div id="comments">
  <%= render @post.comments %>
</div>

<h2>Add a comment:</h2>
<%= render "comments/form" %>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
